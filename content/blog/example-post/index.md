---
title: "An example post"
summary: "One or two sentences shown on the blog listing page."
date: 2026-09-07

# Remove this line to publish. While it is `true` the post is hidden
# from the built site; preview drafts with `hugo server -D`.
draft: true

authors:
  - me

tags:
  - example

# Optional big header image. Delete the whole `cover:` block if unused.
# cover:
#   image: "featured.jpg"     # a file in this folder
#   overlay:
#     enabled: true
#     opacity: 0.4
---

Write the post here in Markdown.

## A section heading

Normal paragraphs, **bold**, *italic*, [links](https://example.org), and lists:

- point one
- point two

Math works too, since KaTeX is enabled: $\pi \approx 3.14159$.

```python
# code blocks are syntax-highlighted
print("hello")
```
